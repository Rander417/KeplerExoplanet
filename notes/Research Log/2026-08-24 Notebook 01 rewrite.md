---
tags: [research-log, notebook-01, rewrite]
date: 2026-08-24
---

# 2026-08-24 Notebook 01 rewrite

> [!success] Outcome
> `notebooks/01_cleaning_eda.ipynb` rewritten on `kepler.data` / `kepler.preprocess` / `kepler.viz`; runs top to bottom on Python 3.12 with no errors or warnings; 11 tests pass, including an exact reproduction of the 2020 `kepler_processed.pkl`.

## What was built

- `src/kepler/data.py` — `load_kaggle_snapshot()` (9,564 × 49, indexed by `kepoi_name`), `load_stellar_extra()`, `error_columns()`, `LABELS`, `LEGACY_NAMES`, explicit class codes (CANDIDATE 0, CONFIRMED 1, FALSE POSITIVE 2).
- `src/kepler/preprocess.py` — `clean()` (no rows dropped; `koi_score_missing`, `has_kepler_name`, delivery dummies), `null_report()`, `rows_with_nulls_by_class()`, named `FEATURE_SETS` (`physics_only`, `with_flags`, `with_flags_and_provenance`, `legacy_2020`), `model_table()` returning features + codes + an account of dropped rows, `legacy_2020_processed()`.
- `src/kepler/viz.py` — project palette and `apply_style()` (see [decision](../Decisions/2026-08-24%20Column%20names,%20palette,%20no%20external%20databases.md)).
- `tests/test_preprocess.py` — 8 tests pinning the numbers below.
- Figures in `reports/figures/eda/`: nulls by column, class balance, field of view, period–radius, insolation–radius with the flat habitable band, Spearman correlation, histogram grid.
- `data/processed/koi_clean.parquet` (regenerated by the notebook; not tracked).

## Numbers (all from this run)

| Fact | Value |
|---|---|
| Rows lost by `dropna` on the 2020 (23-column) feature set | **619** (FP 499, CANDIDATE 112, CONFIRMED 8) |
| Rows lost by `dropna` on the physics-only (12-column) set | **364** — the 2020 provenance columns caused the other 255 |
| KOIs with a Kepler name | 2,294: all 2,293 CONFIRMED plus **one FALSE POSITIVE, K00126.01 = Kepler-469 b** (named, later retracted) |
| `koi_score` missing | 1,510 (exactly the KOIs from the older q1_q16 / dr24 deliveries and those with no delivery) |
| `koi_teq / koi_insol^0.25` | median 255.0 K, 5th–95th percentile 254.7–255.3 K → `koi_teq` duplicates `koi_insol` |
| Confirmed planets inside the flat Sun-like band (0.346–1.020 Earth flux) | 27; **13** with radius ≤ 2 R⊕ (provisional, Phase 4 does it properly) |
| 2020 pipeline reproduced | index, columns and values identical to `kepler_processed.pkl` (atol 1e-9) |

## Notes for the next notebooks

- Notebook 03 should load `koi_clean.parquet` (or call `clean()`), pick a feature set by name, and report macro + per-class f1 for `with_flags` and `physics_only` on the same split; `legacy_2020` gives the 2020 baseline for continuity.
- The physics-only table keeps 9,200 rows; with a NaN-native model it keeps all 9,564.
- Kepler-469 b is a nice example for the deck: a named planet that the archive later reclassified.
