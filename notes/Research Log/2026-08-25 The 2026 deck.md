---
tags: [research-log, presentation, phase-5]
date: 2026-08-25
---

# 2026-08-25 The 2026 deck

> [!success] Outcome
> `reports/presentation/Kepler_Refresh_2026.pptx` (+ `.pdf`), 15 slides, built from the refreshed notebooks and the app. The 2020 deck (`Kepler_Analysis_Presentation.pptx/.pdf`, 16 slides) is untouched and referenced. Regenerate with `node reports/presentation/build_deck.js` (Node.js + `pptxgenjs`; optional — the files next to it are the deliverables).

## Approach

- **New file, old skeleton.** The 2020 deck's arc is kept — mission → three big questions → domain knowledge → EDA and nulls → clustering → two Ys → models → habitable zone → app → tools — and every slide that carried a 2020 number now shows the 2020 claim next to the 2026 measurement ("then / now" cards). The 2020 look (dark background, orange titles) is carried over in the app's dark palette: background `#15140f`, amber titles `#e08a00`, verdict colours green / amber / plum. No blue anywhere, including the figures (all ours).
- **Every number on a slide is in a notebook, `app/data/summary.json` or `reports/tables/`**; the speaker notes name the source per slide. Figures come from `reports/figures/` (notebooks 01, 02, 03, 05, 07, 08) plus two app screenshots saved to `reports/figures/app/`.
- QA: `validate.py` clean; every slide rendered through LibreOffice and inspected; four layout collisions fixed (two-line titles on slides 6 and 8, caption/card overlaps on 12 and 13).

## Slide list

1. Title — credits, repo, app URL (GitHub Pages; the URL is deterministic for the project site and assumes Pages is enabled on this repo).
2. Kepler in one slide — transit method, KOIs, verdicts; our sky map.
3. Three big questions, then and now — 0.75 macro f1 on physics; 16 HZ planets; out-of-time AUC 0.937.
4. The data — snapshot vs live pull; 925 changed verdicts; transition matrix.
5. A tale of two Ys, revisited — the flags are the answer key; 0.86–0.87 vs 0.72; 0.90 vs 0.75 on live labels.
6. Cleaning and EDA — keep all rows; missingness tracks the label; +0.007.
7. Clustering — null result (ARI ≤ 0.05).
8. The models, re-labelled — table: 2020 claim / macro f1 with flags / physics only, incl. the nested-CV boosting (0.722 ± 0.006 snapshot, 0.747 ± 0.012 live).
9. Calibration and the rule — reliability figure; rule; per-class f1; AUC 0.921; log loss 0.534 → 0.512.
10. What the model looks at — permutation importance.
11. The out-of-time test — AUC 0.937; medians 0.95 / 0.26.
12. The habitable zone on insolation — 16 (9 rocky); 0 of the 2020 12 survive; Kepler-1649 b/c caveat.
13. The app — screenshots, three ways to run, Kepler-452 b.
14. What we learned — five takeaways.
15. Tools, credits, references — Kopparapu 2014, Rogers 2015, Mullally 2018, Vanderburg 2020, Angelo 2017.

## Known limitation

The 2020 deck's images of "habitable" planets were gas giants from NASA's Eyes on Exoplanets; the refresh does not reproduce them. Slide 12 shows our own diagram instead.
